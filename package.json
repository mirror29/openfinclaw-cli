{
  "name": "openfinclaw-cli-monorepo",
  "private": true,
  "version": "0.2.3",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @openfinclaw/core build && pnpm --filter @openfinclaw/cli build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @openfinclaw/cli dev",
    "sync-versions": "node scripts/align-package-versions.mjs"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "vitest": "^4.1.4"
  }
}
